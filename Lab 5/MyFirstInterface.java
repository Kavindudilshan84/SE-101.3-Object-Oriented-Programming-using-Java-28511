package va.lab5;

public interface MyFirstInterface {
    int x=10;/*there is no differnce between those two keywords when declaring wihtout 
               public static final keyword it automatically select  that is public static keyword*/
    void display();/* NO
                     if we declare it with or without the abstrsct keyword there is no change will occor  */
}
